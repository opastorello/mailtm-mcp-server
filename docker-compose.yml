services:
  mailtm:
    image: ghcr.io/opastorello/mailtm-mcp-server:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - mailtm-session:/tmp

volumes:
  mailtm-session:
